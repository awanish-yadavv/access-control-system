# Root-level .env — used by docker-compose.yml for variable substitution.
# Copy to .env and fill in your values.
#
# This is NOT the backend or frontend .env.
# It only contains variables referenced directly in docker-compose.yml.

# Your MQTT broker domain (used for the Let's Encrypt TLS cert volume path)
MQTT_DOMAIN=mqtt.neyofit.io
